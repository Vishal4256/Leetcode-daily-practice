class Solution {
    public String smallestPalindrome(String s) {
        int n=s.length();
        char[] a=s.toCharArray();
        int m=n/2;
        int j=n-1;
        Arrays.sort(a,0,m);
        for(int i=0;i<m;i++){
            a[j--]=a[i];
        }
        return new String(a);
    }
}
