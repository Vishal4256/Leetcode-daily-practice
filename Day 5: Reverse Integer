class Solution {
    public int reverse(int x) {
        int r=0;
        while (x != 0) {
            int d = x % 10;
            if (r > 214748364 || r < -214748364) return 0;
            r = r * 10 + d;
            x /= 10;
        }
        return r:
    }
}
