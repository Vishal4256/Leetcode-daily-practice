class Solution {
    public double findMedianSortedArrays(int[] nums1, int[] nums2) {
        int n1=nums1.length;
        int n2=nums2.length;
        int n=n1+n2;
        int[] med=new int[n];
        for(int i=0;i<n1;i++)
        {
            med[i]=nums1[i];
        }
        for(int i=0;i<n2;i++)
        {
            med[n1+i]=nums2[i];
        }
        double ans=0.00;
        Arrays.sort(med);
        int a=((med.length)-1)/2;
        if(med.length%2==0)
        {
            ans= (med[a]+med[a+1])/2.0;
        }
        else{
             ans= med[a];
       }
       return ans;
        
    }
}
